# This is just to switch the order of appearance from BACK-FRONT to FRONT-BACK
cd ./output/
FILES=(*)
for ((i=0;i<${#FILES[*]};i++));
do
    FILES[i]="${FILES[i]// /?}"
done
pdfjoin ${FILES[*]} --outfile ../all_countries.pdf
cd ..
pdf90 all_countries.pdf

