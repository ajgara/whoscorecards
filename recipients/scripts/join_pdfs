# This is just to switch the order of appearance from BACK-FRONT to FRONT-BACK
cd ./output/
FILES=(*)
for ((i=0;i<${#FILES[*]};i++));
do
    FILES[i]="${FILES[i]// /?}"
done
pdfjoin ${FILES[*]} --landscape --outfile ../all_countries.pdf

